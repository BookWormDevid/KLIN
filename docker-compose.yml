version: '3.9'

services:
  klin_api_development:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - web
    environment:
      DATABASE_URL: ${DATABASE_URL}
      RABBIT_URL: ${RABBIT_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.teamtasker_its_api_development.rule=Host(`localhost`)  && PathPrefix(`/api`)"
      - "traefik.http.services.teamtasker_its_api_development.loadbalancer.server.port=8000"
    command:
      [ "/code/.venv/bin/python", "-m", "granian", "--interface", "asgi", "--host", "0.0.0.0", "--port", "8000", "app.presentation.litestar.run:app" ]

  klin_processor_worker_development:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - web
    environment:
      DATABASE_URL: ${DATABASE_URL}
      RABBIT_URL: ${RABBIT_URL}
    command:
      [ "/code/.venv/bin/python", "-m", "faststream", "run", "app.presentation.faststream.app:app" ]
networks:
  web:
    external: true

# frontend:
#     build: ./sandbox/scr/frontend  # Путь к папке, где лежит Dockerfile фронта
#     container_name: klin-frontend
#     ports:
#       - "3000:3000"
#     environment:
#       - WDS_SOCKET_PORT=0 # Чтобы работало живое обновление кода (HMR)
#     volumes:
#       - ./sandbox/scr/frontend:/app             # Прокидываем код для разработки
#       - /app/node_modules           # Исключаем локальные node_modules
#     depends_on:
#       - backend

#   s3-storage:
#     image: minio/minio
#     container_name: klin-s3
#     ports:
#       - "9000:9000"    # API
#       - "9001:9001"    # Консоль
#     environment:
#       MINIO_ROOT_USER: admin
#       MINIO_ROOT_PASSWORD: password123
#     volumes:
#       - ./minio_data:/data
#     command: server /data --console-address ":9001"